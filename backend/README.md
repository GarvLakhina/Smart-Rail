# Backend

This service hosts the core server-side APIs.

- controllers/: Request handlers for each route
- models/: Data models and schemas (e.g., ORM types)
- routes/: Express/FastAPI route definitions
- services/: Business logic, domain services
- utils/: Shared helpers, error handling, middlewares
